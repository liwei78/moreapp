- if @app.errors.any?
  .notification.attention.png_bg
    = link_to image_tag("/images/admin/icons/cross_grey_small.png"), "#", :class => "close"
- form_for(@app, :url => (@app.new_record? ? admin_apps_path : admin_app_path(@app))) do |f|
  %p
    %label App Title
    = f.text_field :title, :class => "text-input medium-input"
  %p
    %label App Description
    = f.text_area :desc, :class => "text-input textarea", :style => "height: 100px;"
  %p
    %label Youtube Url
    = f.text_field :youtube_url, :class => "text-input medium-input"
  %p
    %label App Type
    = f.select :app_type, ["iphone", "ipad"]
  %p
    = submit_tag("Submit", :class => "button")
    
